import PropTypes from 'prop-types'

const Header = () => {
    return (
        <header>
            <h1>PARTICLE SWARM OPTIMIZATION VISUALIZER</h1>
        </header>
    )
}

Header.propTypes = {
}

export default Header
